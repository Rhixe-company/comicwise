Read And understand @/**/*.ts, @/**/*.tsx, @/**/*.mjs, @/**/*.json , @/**/*.mts, @/**/*.json @/**/*.md, @/**/*.txt,   @/**/*.yml,   @/**/*.ps1, @/**/*.sh, @/**/*/Dockerfile, @/src, @scripts and the overall structure of the project.
After fully understanding the project, Request all necessary permissions before proceeding with the tasks if needed. and Confirm if I have the necessary permissions to proceed with these tasks and All the necessary permissions for PowerShell execution, Perform the following  tasks the bests way you recommend, only use a script if manually edits fail or bulk processing is needed and if a script is needed to perform any of the tasks create the script in @/scripts and make sure to optimize then validate before running the script for performance and best practices and document the script with comments for better understanding, the package manager is pnpm  and the system is windows use do not repeat yourself practices dont create a script if not needed. dont create summary or explanation just perform the tasks as requested.
Tasks:
1 - Optimize and Validate with vscode cli if exists update file to an optimized version of @.vscode/mcp.json and start all servers  using the vscode cli
2 - Optimize and Validate with vscode cli if exists update file to an optimized version of @.vscode/extensions.json and install all recommendations extensions  using the vscode cli and  uninstall all unwantedRecommendations extensions  using the vscode cli
3 - Optimize and Validate with vscode cli if exists update file to an optimized version of @.vscode/launch.json
4 - Optimize and Validate with vscode cli if exists update file to an optimized version of @.vscode/tasks.json
5 - Optimize and Validate with vscode cli if exists update file to an optimized version of @.vscode/settings.json
