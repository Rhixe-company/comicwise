Read And understand this project, @.md, @.txt, @.ts, @.tsx, @.mjs, @.json , @.mts, @.json @.yml   @.ps1 @.sh @Dockerfile files, the package manager pnpm, and the overall structure of the project.
After fully understanding the project, the package manager is pnpm  and the system is windows.
Perform the following  tasks the bests way you recommend never use a script.
Please confirm if I have the necessary permissions to proceed with these tasks and confirm if I have the necessary permissions for PowerShell execution use do not repeat yourself practices.
Tasks:
1 - Optimize and Validate with vscode cli if exists rename file to end with .backup then delete the old file and create an enhanced version of @.vscode/mcp.json and start all servers
2 - Optimize and Validate with vscode cli if exists rename file to end with .backup then delete the old file and create an enhanced version of @.vscode/extensions.json and install all recommendations extensions and  uninstall unwantedRecommendations extensions
3 - Optimize and Validate with vscode cli if exists rename file to end with .backup then delete the old file and create an enhanced version of @.vscode/launch.json
4 - Optimize and Validate with vscode cli if exists rename file to end with .backup then delete the old file and create an enhanced version of @.vscode/tasks.json
5 - Optimize and Validate with vscode cli if exists rename file to end with .backup then delete the old file and create an enhanced version of @.vscode/settings.json
