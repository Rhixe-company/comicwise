Read And understand @/**/*.ts, @/**/*.tsx, @/**/*.mjs, @/**/*.json , @/**/*.mts, @/**/*.json @/**/*.md, @/**/*.txt,   @/**/*.yml,   @/**/*.ps1, @/**/*.sh, @/**/*/Dockerfile, @src, @scripts and the overall structure of the project.
After fully understanding the project, Request all necessary permissions before proceeding with the tasks if needed. and Confirm if I have the necessary permissions to proceed with these tasks and All the necessary permissions for PowerShell execution, Perform the following  tasks the bests way you recommend, only use a script if manually edits fail or bulk processing is needed and if a script is needed to perform any of the tasks create the script in @/scripts and make sure to optimize then validate before running the script for performance and best practices and document the script with comments for better understanding, the package manager is pnpm  and the system is windows use do not repeat yourself practices
Tasks:
1 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of @next.config.ts, @eslint.config.ts, @.prettierrc.ts, @postcss.config.mjs, @*config.ts to ensure it follows best practices and is properly configured for this project.
2 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of seeding system in @src/database/seed/**/*.ts use do not repeat yourself practices.
3 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of next-auth user to reflex the user in @src/database/schema.
5 - optimized @src/components/profile/.tsx @src/app/(root)/profile/.ts and implement next-auth crud functionality with reference @src/lib/auth.ts @src/lib/auth*.ts.
6 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of @src/types/*.d.ts @src/types/*.ts  files with  all declarations, reference, modules, types and interfaces for this project to ensure type safety and consistency across the codebase and update all my project types to the new types delete all old or unused types files use do not repeat yourself practices.
7 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of  all  any types to  the correct specific types or update function to be generic with the correct specific types in all @.ts and @.tsx files across the project for better type safety and code quality use do not repeat yourself practices.
8 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of custom paths in @/tsconfig.json for better module resolution and cleaner imports throughout the project use do not repeat yourself practices.
9 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of @/scripts/scripts/replace-imports.ts to ensure all import and export paths are updated according to the new custom paths  defined in @/tsconfig.json then setup an Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version for running the  script .
10 - Optimize and Validate if exists rename file to end with .backup then delete the old file and create an enhanced version of all scripts at @package.json and all powershell and bash scripts in @/scripts to ensure they follow best practices use do not repeat yourself practices.
11 - Create and Run once created and validated  a scripts to Perform an update all  @.ts, @.tsx files and functions to   use CamelCase change filename to use CamelCase where applicable for better readability and consistency, Refactoring of the folder structure for better maintainability, next@latest in @src and Create and Run once created and validated  a scripts to Perform a cleanup of @. and @src by prevent duplicates use do not repeat practices deleting unused or duplecate files and functions and all files that ends with .backup and delete all unused @src/components that are not being used by any files in the @src/app and optimize the folder structure for better organization
12 - uninstall all its packages from @package.json.
13 - Fix all type-check and linting errors and warnings manually use do not repeat yourself practices.
14 - Create an Optimize .github/prompts/Setup.prompt.md if exists update the file  with all the content,tasks, todos, recommendations from @.md, @.txt, @.ts, @.tsx, @.mjs, @.json , @.mts, @.json @.yml   @.ps1 @.sh @Dockerfile files  as Github copilot cli Prompts for a complete setup of this project and scaffolding all necessary files, components, pages and codes   setup handler for long running functions.
15 - Create a comprehensive README.md for the project that includes setup instructions, usage guidelines, contribution instructions, and any other relevant information.
